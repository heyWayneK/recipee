describe("Navigation", () => {
  it("loads homepage and navigates to about page", () => {
    // cy.visit("/");
    // cy.contains("Welcome").should("be.visible"); // Adjust to your homepage content
    // cy.get("a[href='/recipee']").click();
    // cy.url().should("include", "/recipee/customer");
    // cy.contains("Customer").should("be.visible"); // Adjust to your about page content
  });
});
